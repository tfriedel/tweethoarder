# .config/wt.toml - Worktrunk project hooks
# See: https://worktrunk.dev/

[post-create]
# Copy env file from main worktree
env = "[ -f '{{ main_worktree_path }}/.env' ] && cp '{{ main_worktree_path }}/.env' .env || true"
# Install all dependencies
setup = "just setup"

[pre-merge]
ci = "just ci"
